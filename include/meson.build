# headers
if not meson.is_subproject()
  install_headers(
    files([
      'aligner.h',
      'score.h',
      'seed.h',
      'trace.h',
      'vectors.h',
      'xavier.h',
      ]))
endif

xavier_include_directories = include_directories(['.'])
